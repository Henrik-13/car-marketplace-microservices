apiVersion: v1
kind: Service
metadata:
  name: user-db
  namespace: databases
  labels:
    app.kubernetes.io/name: user-db
    app.kubernetes.io/part-of: car-marketplace
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: user-db
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
