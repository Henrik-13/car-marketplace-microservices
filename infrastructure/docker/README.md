# Docker Notes

Service Dockerfiles are located in:

- `services/user-service/Dockerfile`
- `services/listing-service/Dockerfile`
- `services/prediction-service/Dockerfile`

Use the repository root `docker-compose.yml` for local multi-service development.
